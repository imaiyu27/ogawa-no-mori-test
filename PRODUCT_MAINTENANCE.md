# 商品マスタ運用手順

## 初回設定

1. 予約用Googleスプレッドシートを開く。
2. 下部の `+` で新しいシートを追加する。
3. シート名を `商品マスタ` に変更する。
4. `product_master_initial.tsv` の中身をコピーする。
5. `商品マスタ` シートの `A1` セルを選択して貼り付ける。
6. Apps Scriptのコードを `apps_script_code.gs` の内容に差し替える。
7. Apps Scriptで `デプロイ` → `デプロイを管理` → 鉛筆アイコン → `新バージョン` → `デプロイ`。
8. GitHub Desktopでこのリポジトリを `Commit to main` → `Push origin`。

## 日常運用

- 商品追加: `商品マスタ` に1行追加する。
- 商品修正: 該当行のセルを編集する。
- 売切れ: `表示状態` を `売切れ` にする。
- 非表示: `表示状態` を `非表示` にする。
- 表示順変更: `表示順` の数字を変える。小さい数字ほど先に表示される。
- 画像差し替え: GitHubの `images` フォルダに画像を追加し、`画像URL` に `images/ファイル名.png` を入力する。

## 表示状態

- `表示`: ページに表示され、予約できます。
- `売切れ`: ページに表示されますが、予約ボタンは押せません。
- `非表示`: ページに表示されません。

## 注意

`商品ID` は予約データと突き合わせるためのキーです。既存商品の `商品ID` はなるべく変更しないでください。

サンプル画像は `images/BR-202605-001.png` のように商品ID名で保存しています。
